@model LostPets.Web.Areas.Administration.ViewModels.AdminComments.AdminEditCommentViewModel
@{
    ViewBag.Title = "Edit Comment";
}
@using (Html.BeginForm("EditComment", "AdminComments", FormMethod.Post, new { id = Model.Id, @class = "form", enctype = "multipart/form-data" }))
{
    <div class="container">
        <div class="=row">
            @Html.AntiForgeryToken()
            <br />
            <div class="col-lg-6">
                <div class="row">
                    <div class="row">
                        @Html.LabelFor(m => m.Content)
                    </div>
                    <div class="row">
                        @Html.TextAreaFor(m => m.Content, new { @class = "form-control", @value = "@Model.Content" })
                    </div>
                    <div>
                        @Html.ValidationMessageFor(m => m)
                    </div>
                </div>
            </div>
            <div class="co-lg-5">
                <br />
                <br />
                <input type="submit" value="Edit Comment" class="btn btn-primary" />
            </div>
        </div>
    </div>
}

@section scripts {
    @Scripts.Render("~/bundles/jqueryval")
}

